import time

def measure_latency(encoder, frame):
    start = time.time()
    _ = encoder.encode(frame)
    return (time.time() - start) * 1000

def solve_with_fallback(problem, quantum_solver, classical_solver):
    try:
        return quantum_solver.solve(problem)
    except Exception:
        return classical_solver.solve(problem)

